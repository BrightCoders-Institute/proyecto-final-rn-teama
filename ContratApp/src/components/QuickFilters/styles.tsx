import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    width: '100%',
    borderRadius: 50,
    paddingVertical: 10,
  },
  gradientContainer: {
    position: 'absolute',
    marginLeft: '80%',
  },
});
